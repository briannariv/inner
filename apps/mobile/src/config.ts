// Points the app at the backend. localhost only works in a web/simulator
// context that shares the host's network stack — testing on a physical
// device or Android emulator means swapping this to your machine's LAN IP
// (or a deployed API URL) before running `expo start`.
export const API_BASE_URL = "http://localhost:4000";
